from setuptools import setup, find_packages

setup(
    name='cs7643-fastmri',
    version='0.1',
    packages=find_packages(),
    install_requires=[],
    include_package_data=True,
    zip_safe=False,
    entry_points={},
)